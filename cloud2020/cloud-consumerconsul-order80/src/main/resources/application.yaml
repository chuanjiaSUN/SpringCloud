server:
  port: 80

spring:
  application:
    name: cloud-order-service
  cloud:
    consul:
      host: 120.26.170.124
      port: 8500
      discovery:
        heartbeat:
          enabled: true
        service-name: ${spring.application.name}